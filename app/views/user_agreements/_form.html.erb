<%= simple_form_for(@user_agreement) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :comment %>
    <%= f.input :text, as: :text %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
